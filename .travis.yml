language: java
jdk:
  - oraclejdk8
cache:
  directories:
    - "$HOME/.gradle/caches/"
    - "$HOME/.gradle/wrapper/"
addons:
  apt:
    packages:
      - oracle-java8-installer
install: true
before_script:
  - chmod +x gradlew
script: ./gradlew --no-daemon