package com.koval.jresolver.rules

import com.koval.jresolver.common.api.bean.issue.Issue;

global java.util.List results

rule "Check status"
	dialect "mvel"
	when
		Issue( status == "CLOSED" )
	then
		results.add("Status is closed");
end